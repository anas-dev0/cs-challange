# Docker Environment Variables
# Copy this file to .env in the project root for Docker Compose

# PostgreSQL Database Configuration
POSTGRES_USER=postgres
POSTGRES_PASSWORD=change_this_secure_password_123!
POSTGRES_DB=utopiahire
POSTGRES_PORT=5432

# PgAdmin Configuration
PGADMIN_EMAIL=admin@utopiahire.local
PGADMIN_PASSWORD=change_this_secure_password_123!
PGADMIN_PORT=5050

# SECURITY NOTES:
# 1. ALWAYS change default passwords in production
# 2. Use strong passwords with at least 16 characters
# 3. Consider using Docker secrets for production deployments
# 4. Never commit this file with real credentials to version control
# 5. In production, bind database ports to localhost only (127.0.0.1:5432:5432)
