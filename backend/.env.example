# =============================================================================
# SECURITY CONFIGURATION - READ THIS FIRST!
# =============================================================================
# CRITICAL: Change ALL default values before deploying to production
# Use strong, randomly generated values for all secrets
# Never commit .env files with real credentials to version control
# Consider using environment variables or secret management services
# =============================================================================

# JWT and Authentication Security
# Generate strong secrets using: python -c "import secrets; print(secrets.token_urlsafe(32))"
JWT_SECRET=your-super-secret-jwt-key-change-in-production
REFRESH_SECRET=your-super-secret-refresh-key-also-change-in-production
SESSION_SECRET=your-session-secret-for-oauth
ACCESS_EXPIRES_HOURS=2
REFRESH_EXPIRES_DAYS=7

# Email Configuration (for verification emails and notifications)
SMTP_SERVER=smtp.gmail.com
SMTP_PORT=587
SENDER_EMAIL=your-email@gmail.com
SENDER_PASSWORD=your-app-specific-password
SENDER_NAME=AI Interview Coach

# Database Configuration
# SECURITY: Use strong passwords and consider connection pooling limits
# Format: postgresql+asyncpg://username:password@host:port/database
DATABASE_URL=postgresql+asyncpg://postgres:change_this_secure_password_123!@localhost:5432/utopiahire

# CORS Configuration
# SECURITY: Restrict to your actual frontend domains in production
# Separate multiple origins with commas
CORS_ORIGINS=http://localhost:5173,http://localhost:5174,http://localhost:3000

# OAuth Credentials (Google)
# Get credentials from: https://console.cloud.google.com/
GOOGLE_CLIENT_ID=your-google-client-id.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=your-google-client-secret
GOOGLE_REDIRECT_URI=http://localhost:8000/api/auth/oauth/google/callback

# OAuth Credentials (GitHub)
# Get credentials from: https://github.com/settings/developers
GITHUB_CLIENT_ID=your-github-client-id
GITHUB_CLIENT_SECRET=your-github-client-secret
GITHUB_REDIRECT_URI=http://localhost:8000/api/auth/oauth/github/callback

# Frontend URL
# Used for email verification links and OAuth redirects
FRONTEND_URL=http://localhost:5173

# LiveKit Configuration
# Get credentials from: https://cloud.livekit.io/
LIVEKIT_URL=wss://your-livekit-instance.livekit.cloud
LIVEKIT_API_KEY=your-livekit-api-key
LIVEKIT_API_SECRET=your-livekit-api-secret

# Google Gemini AI API
# Get API key from: https://ai.google.dev/
GOOGLE_API_KEY=your-google-gemini-api-key

# =============================================================================
# SECURITY CHECKLIST FOR PRODUCTION:
# =============================================================================
# [ ] Changed all default passwords and secrets
# [ ] Used strong random values (32+ characters)
# [ ] Configured HTTPS and updated all URLs to https://
# [ ] Set secure cookies (secure=True in middleware)
# [ ] Restricted CORS_ORIGINS to actual domains only
# [ ] Enabled database SSL connections
# [ ] Set up database backups
# [ ] Configured rate limiting appropriately
# [ ] Reviewed and tested all security headers
# [ ] Set up monitoring and alerting
# [ ] Configured proper file upload limits
# [ ] Tested OAuth callback URLs
# [ ] Set up proper logging (without sensitive data)
# =============================================================================
